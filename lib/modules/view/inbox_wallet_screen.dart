import 'package:flutter/material.dart';

class InboxWalletScreen extends StatelessWidget {
  const InboxWalletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('InboxWallet'),
      ),
      body: const Center(
        child: Text('InboxWallet Screen'),
      ),
    );
  }
}
